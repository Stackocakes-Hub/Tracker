# GitHub token with contents:read/write on Stackocakes-Hub/Tracker-Vault
GH_TOKEN=
# optional alias
TRACKER_GH_TOKEN=

# Vercel Blob (published picture store for the website UI). Optional locally.
BLOB_READ_WRITE_TOKEN=
